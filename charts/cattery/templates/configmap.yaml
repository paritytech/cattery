apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cattery.fullname" . }}-config
  labels:
    {{- include "cattery.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
